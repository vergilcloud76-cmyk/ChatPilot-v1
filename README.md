# ChatPilot 🚀

**ChatPilot** هو بوت تفاعلي ذكي لإدارة الرسائل، الرد على التعليقات، وإرسال الرسائل التلقائية للتجار، يعمل على Telegram وFacebook مع واجهة Dashboard لإدارة الحسابات والقواعد بسهولة.

---

## المميزات

* الرد التلقائي على الرسائل والتعليقات.
* تسجيل الرسائل والتفاعل مع العملاء.
* إدارة قواعد الرد حسب الكلمات أو الأحداث.
* واجهة Dashboard لمراقبة الحسابات والتقارير.
* دعم Telegram وFacebook (Messenger).

---

## المتطلبات

* Node.js v20+
* npm v10+
* MongoDB (Atlas أو محلي)
* حساب Telegram Bot API (لتجربة Telegram)

---

## الإعداد

1. **نسخ المشروع:**

```bash
git clone https://github.com/vergilcloud76-cmyk/ChatPilot-v1.git
cd ChatPilot
```

2. **تثبيت الحزم:**

```bash
npm install
```

3. **إعداد ملف البيئة `.env`:**
   انسخ `.env.example` إلى `.env`:

```bash
cp .env.example .env
```

ثم عدّل القيم:

```env
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname
PORT=10000
OPENAI_API_KEY=your_openai_api_key_here
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
```

> **مهم:** لا ترفع المفتاح الحقيقي إلى GitHub.

4. **تشغيل السيرفر:**

```bash
npm start
```

* السيرفر سيعمل على: `http://localhost:10000`

---

## تشغيل Docker (اختياري)

1. **بناء الصورة:**

```bash
docker build -t chatpilot .
```

2. **تشغيل الحاوية:**

```bash
docker run -d -p 10000:10000 --env-file .env chatpilot
```

---

## الهيكلية

```
ChatPilot/
│
├─ server/
│   ├─ index.js
│   ├─ routes/
│   │   ├─ auth.js
│   │   ├─ merchant.js
│   │   ├─ webhook.js
│   │   └─ admin.js
│   ├─ models/
│   │   ├─ Merchant.js
│   │   ├─ Rule.js
│   │   └─ MessageLog.js
│   └─ utils/
│       ├─ openai.js
│       ├─ telegram.js
│       └─ jwt.js
│
├─ dashboard/
│   ├─ package.json
│   └─ src/
│       ├─ App.jsx
│       ├─ pages/
│       │   ├─ Login.jsx
│       │   └─ Dashboard.jsx
│       └─ components/
│           ├─ MerchantForm.jsx
│           ├─ RuleManager.jsx
│           └─ MessageLog.jsx
│
├─ .env.example
├─ package.json
├─ deploy.sh
├─ Dockerfile
└─ docker-compose.yml
```

---

## الملاحظات

* لا ترفع **مفاتيح API حقيقية** إلى GitHub، استخدم `.env` محليًا.
* يمكن تجربة البوت أولًا على Telegram قبل ربطه بالفيسبوك.
* كل تحديثات القواعد والردود تتم من خلال Dashboard مباشرة.

---

إذا تحب، أقدر أصنع لك نسخة **Markdown جاهزة تمامًا للـ GitHub** مع روابط مباشرة لكل قسم، بحيث تصبح مستودع جاهز للنشر بدون أي مشاكل.

هل تريد أن أفعل ذلك؟
